{
  "name": "spookys-skyrimnet-prompt-tuner",
  "version": "1.3.0",
  "description": "Spooky's SkyrimNet Prompt Tuner — prompt engineering tool for SkyrimNet AI NPCs",
  "main": "electron/main.js",
  "author": "Spooky",
  "license": "MIT",
  "scripts": {
    "dev": "cd prompt-tuner && npm run dev",
    "build:next": "cd prompt-tuner && npm run build && cd .. && node scripts/copy-static.js",
    "package": "npm run build:next && electron-builder --win",
    "package:fast": "electron-builder --win"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3"
  }
}
